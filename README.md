
  # ZConnect

  This is a code bundle for ZConnect. The original project is available at https://www.figma.com/design/SZNNFWZU6ZbP9R2cAkn0lM/ZConnect.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  